<%= render partial: 'employee_lists', locals: { state: 'current',
                                              store: @employee.current_assignment.store.name,
                                                  employees: @current_employees } %>
